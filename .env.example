# Environment Configuration for WhatsApp Supplier Bot

# Node Environment
NODE_ENV=development

# Cache Settings
# How long to cache geocoding results (in hours)
CACHE_EXPIRY_HOURS=24

# Note: This bot uses OpenStreetMap (Nominatim) for geocoding
# No API key is required for geocoding services
